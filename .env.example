# Used only by MCP clients; do not commit real credentials.
SUPABASE_PROJECT_REF=zglbgqeccebqnijcqfkb
SUPABASE_ACCESS_TOKEN=
VITE_SUPABASE_URL=https://zglbgqeccebqnijcqfkb.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SCENA_APP_URL=https://scena.kpnsolute.com

# Native Supabase Auth. Google OAuth must be enabled for this project in the
# Supabase dashboard (Authentication > Providers > Google) — that provider
# configuration is not something this repo or its migrations can set.
# No client secret belongs here: Supabase's server side holds it.

# This Scena Supabase project's own service-role credentials, used by Edge
# Functions to provision accounts, Teams, and billing records.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# billing-checkout / billing-portal / billing-webhook Edge Function secrets
# (set via Supabase dashboard function secrets, not here).
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
SCENA_APP_URL=
